# Integration Tests

Empty by design. The agent writes tests here as part of the outer loop.

These tests should exercise the feature end-to-end — boot a real server, hit it over HTTP, assert on the response. No mocking of the feature itself.

See `../../SETUP.md` for the outer-loop prompt.
