# Unit Tests

Empty by design. The agent writes tests here as part of the inner loop.

Naming convention: `test_<module>.py` for each module under `app/`.

See `../../SETUP.md` for the inner-loop prompt.
